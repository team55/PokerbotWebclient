
Todo's:
  - tutorials
    - uitleg per tutorial stap
    - meerdere manieren om tutorial op te lossen?
    - hand-checker (eerst xml?)
  - Choose some cooler colors!

  - Automatisch plaatsnemen aan een tafel na het aanmaken ervan.
  - Titel voor de grafiek bovenaan.
  - rekentijd als onderste grafiek zetten
  - categorieen sorteren op popu, kaarten acties ook meer naar boven

  - Move the XML out of the main.html files.

Notes for demo:

  BLOCKLY
  - Design of BLOCKLY is pretty ugly, but couldn't find a decent color palette
    for now. Should still be updated in the future.

  CREATE TABLES / SIGN IN
  - Double check password? Making a mistake is possible for now.
  - There is no error if the table already exists...
  - Loading icon when signing in... Can take a while.
  - For now, table selection is done with an input box but would be cooler with
    a dropdown. In order to implement this, we need to fix the backend API.

  GRAPHS
  - Fixed a rounding issue... Had too much detail!
  - What is the basic rule? Weird for first graph...

  GENERAL
  - Errors should be written in Dutch (server-side).
  - We can't see our rules atm. We can save these client side?

  ISSUES
  - When focus is on an input box, border-radius magically disappears?
  - Load blocks issue... Remove buttons?


// Future?:

Ideas:
- Uploading BLOCKLY code to preview others? (db?)
- More Stepwise Client Side
- Load bots into the tables
- Preconfigured in a list (the default bots)
- Lock tables (hide tables) and unlock tables
- Explanation of the graphs

Tutorial steps:
1.  Callbot en if statement uitleggen
    Make a high card (with examples)
2.  Make a pair, variables uitleggen
3.  #Raises om te limiteren
4. volgorde van statements, evt me de 3 of a kind
5. defeat bot
